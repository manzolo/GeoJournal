{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *strings.xml* ]] && cd /home/manzolo/AndroidStudioProjects/GeoJournal && bash scripts/check_strings_parity.sh; } 2>/dev/null || true",
            "statusMessage": "Verifica parità IT/EN strings..."
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.kt ]] && bash /home/manzolo/AndroidStudioProjects/GeoJournal/scripts/check_hilt_import.sh \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Verifica import hiltViewModel..."
          }
        ]
      }
    ]
  }
}
